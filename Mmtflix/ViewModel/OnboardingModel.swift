import UIKit

class Onboarding {
    
    var titleLabel:String?
    var image:UIImage?
    
    init() {
    }
    
    init(titleLabel:String,image:UIImage) {
        self.titleLabel = titleLabel
        self.image = image
    }
}
